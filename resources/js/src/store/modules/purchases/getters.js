export const loading = state => state.loading;

export const errors = state => state.errors;

export const purchasesList = state => state.purchasesList;

export const selectedPurchase = state => state.selectedPurchase;
