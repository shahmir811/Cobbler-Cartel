export const loading = state => state.loading;

export const errors = state => state.errors;

export const inventoryList = state => state.inventoryList;

export const selectedInventory = state => state.selectedInventory;
